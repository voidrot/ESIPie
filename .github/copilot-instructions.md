# ESIPie Copilot Instructions

# Testing
- Always ensure that test files are created in the `tests/` directory at the root of the project.
- Use `pytest` for writing and running tests.
- Mock external dependencies to isolate unit tests.
- Ensure that tests are and the test logic is documented with comments.
